# Coding Standards

## Comment Guidelines
- Never use numerical literals in comments
- Always reference variable names instead
- Example: Use `// Process ${maxRetries} attempts` not `// Process 3 attempts`

## Code Style
- C++ code should be camelCase
- Python code shouldb be snake_case

